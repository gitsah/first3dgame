using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//commented out until further notice, will likely handle the weapon from the player manager and the swapping from the weaponbox
/*//This class will keep track of the currently equiped weapon and picking up and swapping weapons, the PlayerManager will draw the weapon stats from here
public class WeaponManager : MonoBehaviour{
    Weapon currentWeapon;
    Weapon swapOption;
    //GameObject player; shouldn't need this if this script is attached to the player gameObject
    GameObject floorWeapon;

    private void Update()
    {
        if(Vector3.Distance(transform.position, floorWeapon.transform.position) < 4)
        {
            //todo: add weapon pickup display here, something like "press x to pickup weapon"
            //todo: if e is pressed the current Weapon will be switched for the Weapon that is being highlighted on the ground
        }
    }

}*/
